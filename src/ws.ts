import socket = require('socket.io-client');

export class Ws {

  constructor() {
    // TODO?
  }

  // public subscribe(channel: string, callback: string) {
  //   const sock = socket('http://localhost:8080');
  //   sock.on(channel, callback);
  // }

}
